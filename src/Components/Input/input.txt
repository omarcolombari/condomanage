import { Box, Input } from "@chakra-ui/react";

const Inputs = ({ register,bgColor,phColor,registerData }) => {
    return(
        <Box
            bg= {bgColor}
            w="95%"
            borderRadius="30px">
            <Input 
                variant="outline"
                placeholder="teste"
                focusBorderColor='transparent'
                _placeholder={{ opacity: 1,color: phColor }}/>
        </Box>
    )
}

export default Inputs;
